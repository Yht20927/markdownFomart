---
name: md-table-to-csv
description: Extract Markdown tables to CSV format with pipe-char escaping
triggers:
  - Extract table from Markdown to CSV
  - Markdown 表格转 CSV
  - Convert md table to csv
  - Export markdown table as spreadsheet
---

# md-table-to-csv Skill

Extract Markdown tables to CSV format. Handles embedded pipe characters in cells. Same approach as md-to.com.

## Usage

```bash
node index.js convert -i input.md -o output.csv
```
