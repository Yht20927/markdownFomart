---
name: json-to-md
description: JSON array to Markdown table conversion with nested data path detection
triggers:
  - Convert JSON to Markdown table
  - JSON 转 Markdown 表格
  - Generate markdown table from JSON
  - json to md table
---

# json-to-md Skill

JSON arrays to Markdown tables. Auto-detects nested data paths (data.data, data.rows, data.items). Same approach as md-to.com.

## Usage

```bash
node index.js convert -i input.json -o output.md
```
