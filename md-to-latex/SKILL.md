---
name: md-to-latex
description: Convert Markdown to LaTeX with full document template for academic papers
triggers:
  - Convert Markdown to LaTeX
  - Markdown 转 LaTeX
  - Export markdown to tex
  - md to latex for academic paper
  - Generate LaTeX document from markdown
---

# md-to-latex Skill

Markdown to LaTeX for academic papers. Generates complete `.tex` document with listings, hyperref, and graphicx packages. Same approach as md-to.com.

## Usage

```bash
node index.js convert -i input.md -o output.tex
```
